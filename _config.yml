# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing these this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Korutx's Blog
email: korutx@gmail.com
description: > # this means to ignore newlines until "baseurl:"
  Blog sobre tecnología y alguna que otra reflexión, comentado desde un punto de vista muy personal.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://korutx.github.io" # the base hostname & protocol for your site
twitter_username: korutx
github_username:  korutx
baseurl:     '' # The subpath of your site, e.g. /blog
twitter:     ''
github:      'korutx'
instagram:   'korutx'
facebook:    ''

markdown:  kramdown
remote_theme: aspirethemes/type
permalink: pretty
paginate:  60

sass:
  style: compressed

plugins:
  - jekyll-paginate
  - jekyll-tagging

include:
  - _pages

exclude:
  - vendor
  - Gemfile
  - Gemfile.lock

# Tags
tag_page_dir:         tag
tag_page_layout:      tag_page
tag_permalink_style:  pretty

# Pages path
defaults:
  - scope:
      path: '_pages'
    values:
      permalink: /:basename:output_ext
host: 0.0.0.0
